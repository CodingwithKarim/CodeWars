function maps(x) {
  const arr = [];
  for (let i = 0; i < x.length; i++) {
    arr.push(x[i] * 2);
  }
  return arr;
}
//empty array, for loop for index of array
//each loop pushes x[i] * 2 to the arr variable
//return arr;
