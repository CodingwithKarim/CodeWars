function divide(weight) {
  if (weight % 2 === 0 && weight > 2) {
    return true;
  } else {
    return false;
  }
}
//check to see if weight is even and ensure weight is above 2 so it can be added together by two even numbers
