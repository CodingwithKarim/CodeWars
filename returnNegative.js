function makeNegative(num) {
  if (num >= 0) {
    return num * -1;
  } else if (num < 0) {
    return num;
  }
}
//if else for postive and negative numbers with different return methods for each if statement
